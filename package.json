{
  "name": "omarchy-listening-post",
  "version": "1.2.0",
  "private": true,
  "license": "MIT",
  "description": "Listening Post widget for the Omarchy bar - node is used ONLY to run the offline unit suite; the shipped plugin requires no node runtime",
  "scripts": {
    "test": "c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 --statements 95 node --test tests/*.test.js",
    "test:race": "for run in 1 2 3; do node --test --test-concurrency=4 tests/*.test.js || exit; done",
    "test:mutation": "stryker run",
    "test:e2e": "bash e2e/buzz.sh",
    "audit": "audit-harness verify && audit-harness audit . --deep && audit-harness scan ."
  },
  "devDependencies": {
    "@intentsolutions/audit-harness": "1.3.1",
    "@stryker-mutator/core": "10.0.0",
    "c8": "12.0.0"
  },
  "overrides": {
    "qs": "6.15.3"
  }
}
