{
  "name": "omarchy-listening-post",
  "version": "1.0.0",
  "private": true,
  "description": "Listening Post widget for the Omarchy bar - dev harness for the Model.js data layer",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
