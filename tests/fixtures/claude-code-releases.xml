<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/" xml:lang="en-US">
  <id>tag:github.com,2008:https://github.com/anthropics/claude-code/releases</id>
  <link type="text/html" rel="alternate" href="https://github.com/anthropics/claude-code/releases"/>
  <link type="application/atom+xml" rel="self" href="https://github.com/anthropics/claude-code/releases.atom"/>
  <title>Release notes from claude-code</title>
  <updated>2026-08-20T20:33:43Z</updated>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.238</id>
    <updated>2026-08-20T20:33:51Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.238"/>
    <title>v2.1.238</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Added a &lt;code&gt;keybindingFlavor&lt;/code&gt; setting: set it to &lt;code&gt;&quot;readline&quot;&lt;/code&gt; to make Ctrl+W in the prompt delete back to the previous whitespace, as in Bash; the default (&lt;code&gt;&quot;classic&quot;&lt;/code&gt;) is unchanged&lt;/li&gt;
&lt;li&gt;Plugin marketplaces: &lt;code&gt;headersHelper&lt;/code&gt; on a url marketplace or a catalog entry runs a command that mints HTTP headers (e.g. a short-lived token) for catalog and same-origin archive fetches&lt;/li&gt;
&lt;li&gt;A catalog entry&#39;s &lt;code&gt;headersHelper&lt;/code&gt; runs only when you install or update that plugin, after its command is shown; &lt;code&gt;claude plugin install/update&lt;/code&gt; ask &lt;code&gt;[y/N]&lt;/code&gt; (or pass &lt;code&gt;-y&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Added &lt;code&gt;claude self-hosted-runner --defer-shutdown-max-min &amp;lt;minutes&amp;gt;&lt;/code&gt;: on SIGTERM, keep serving attached sessions, park what is left after that many minutes, then exit&lt;/li&gt;
&lt;li&gt;Added &lt;code&gt;claude self-hosted-runner --proxy-authorization-command&lt;/code&gt; / &lt;code&gt;--proxy-authorization-file&lt;/code&gt; for egress proxies that require a freshly issued &lt;code&gt;Proxy-Authorization&lt;/code&gt; header on every connection&lt;/li&gt;
&lt;li&gt;Fixed unbounded memory growth in long interactive sessions: subagent tool results are now released once they leave the recent display window&lt;/li&gt;
&lt;li&gt;Fixed custom, project, and plugin output styles drifting back to the default voice mid-session&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION=true&lt;/code&gt; not keeping prompt suggestions on when your account is near, but not over, its usage limit&lt;/li&gt;
&lt;li&gt;Fixed worktree-isolation Bash refusals telling you to remove a redirect when the command had none&lt;/li&gt;
&lt;li&gt;Fixed self-hosted runners occasionally being removed by the server after a single slow or lost poll request, handing their healthy session to another runner&lt;/li&gt;
&lt;li&gt;Fixed MCP elicitation dialogs showing nothing for URLs longer than 4,096 characters, and permission prompts dropping the &quot;don&#39;t ask again&quot; option when the project path didn&#39;t fit the terminal width&lt;/li&gt;
&lt;li&gt;Fixed leftover &lt;code&gt;/tmp/claude-*-cwd&lt;/code&gt; files when a Bash command is killed, times out, or is interrupted&lt;/li&gt;
&lt;li&gt;Fixed held Backspace being ignored on terminals that send Ctrl+H for Backspace when keystrokes arrive in large bursts (slow SSH/mosh links)&lt;/li&gt;
&lt;li&gt;Fixed text-wrapping in permission prompt diffs: lines containing wide multi-code-point characters (such as emoji) or tabs are no longer clipped&lt;/li&gt;
&lt;li&gt;Fixed killing a suspended (Ctrl+Z) session sometimes leaving the terminal in bracketed-paste mode with the cursor hidden&lt;/li&gt;
&lt;li&gt;Fixed stdio MCP servers receiving a &lt;code&gt;server/discover&lt;/code&gt; request before &lt;code&gt;initialize&lt;/code&gt;, forcing lazy servers to start their backend on every session open&lt;/li&gt;
&lt;li&gt;Fixed a proxy&#39;s refusal of a connection being reported as a generic network error instead of naming the proxy&lt;/li&gt;
&lt;li&gt;Fixed the &lt;code&gt;/model&lt;/code&gt; and &lt;code&gt;/effort&lt;/code&gt; cache-miss warning appearing when the prompt cache had already expired&lt;/li&gt;
&lt;li&gt;Fixed per-task Stop from the Remote Control tasks panel doing nothing on CLI-hosted sessions&lt;/li&gt;
&lt;li&gt;Fixed remote sessions exiting when a client delivered a user message without a valid role&lt;/li&gt;
&lt;li&gt;Fixed Remote Control sessions started by &lt;code&gt;claude remote-control&lt;/code&gt; inheriting session-scoped environment variables from the launching shell&lt;/li&gt;
&lt;li&gt;Fixed a Remote Control session whose process crashed staying unavailable until &lt;code&gt;claude remote-control&lt;/code&gt; was restarted; it can now be reused when you next message it&lt;/li&gt;
&lt;li&gt;Fixed Remote Control messages sent from the web or Desktop while Claude is mid-turn disappearing from the transcript after the turn finishes&lt;/li&gt;
&lt;li&gt;Fixed Remote Control model picks made on a phone or web not updating the model shown in the terminal&lt;/li&gt;
&lt;li&gt;Fixed Remote Control disconnecting with &quot;login expired&quot; when a brief network hiccup delays renewing your sign-in; it now retries and stays connected&lt;/li&gt;
&lt;li&gt;Fixed Remote Control reporting a failed reconnect on sign-out; signing out now ends the session with a clear message&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;ListAgents&lt;/code&gt;/&lt;code&gt;SendMessage&lt;/code&gt; reporting &quot;Remote Control is not connected&quot; in sessions run by &lt;code&gt;claude remote-control&lt;/code&gt; (server mode) or Desktop/IDE hosts; they now list and reach Remote Control peers&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;ListAgents&lt;/code&gt; and &lt;code&gt;SendMessage&lt;/code&gt; exposing the idle worker that the agent view pre-warms for your next background session; it now appears only once a task claims it&lt;/li&gt;
&lt;li&gt;Cross-session messaging: sending to a session on this machine that refuses inbound messages (e.g. &lt;code&gt;crossSessionInbound: &quot;refuse&quot;&lt;/code&gt;) now reports &quot;refused&quot; to the sender instead of a silent success&lt;/li&gt;
&lt;li&gt;Cross-session messaging: a session whose inbox drops your messages (rate limit or full queue) now tells your session, instead of the messages vanishing silently&lt;/li&gt;
&lt;li&gt;Improved startup: bare &lt;code&gt;claude&lt;/code&gt; starts sooner on macOS&lt;/li&gt;
&lt;li&gt;Improved Bash tool permission checking for zsh-specific syntax in shell conditionals&lt;/li&gt;
&lt;li&gt;Improved Remote Control connection resilience: brief HTTP 403 refusals from a network edge, VPN, or proxy are now tolerated for up to 3 minutes, with the refusing party named when a block persists&lt;/li&gt;
&lt;li&gt;Improved startup responsiveness: the automatic update check now runs about 10 seconds after launch instead of competing with startup for CPU&lt;/li&gt;
&lt;li&gt;Updated the bundled &lt;code&gt;claude-api&lt;/code&gt; skill for the Managed Agents Aug 19 release: web search/fetch domain settings and memory stores on self-hosted sandboxes&lt;/li&gt;
&lt;li&gt;Changed Ctrl+L and Cmd+K in fullscreen to always just repaint — the double-press &lt;code&gt;/clear&lt;/code&gt; shortcut was removed, and 1-row nvim terminals no longer trigger automatic &lt;code&gt;/clear&lt;/code&gt; loops&lt;/li&gt;
&lt;li&gt;Changed &lt;code&gt;claude mcp list&lt;/code&gt; and &lt;code&gt;claude mcp get&lt;/code&gt; to show disabled servers as &lt;code&gt;⊘ Disabled&lt;/code&gt; instead of connecting to them for a health check&lt;/li&gt;
&lt;li&gt;MCP &lt;code&gt;headersHelper&lt;/code&gt; in a project &lt;code&gt;.mcp.json&lt;/code&gt;, and inline MCP servers in project or &lt;code&gt;--add-dir&lt;/code&gt; agent files, now require that folder&#39;s trust dialog to have been accepted (also under &lt;code&gt;claude -p&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;MCP &lt;code&gt;headersHelper&lt;/code&gt; from a project &lt;code&gt;.mcp.json&lt;/code&gt;, plugin, or agent file runs without inherited credential env vars; user, managed and claude.ai-scope helpers now run from the Claude config dir&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.237</id>
    <updated>2026-08-20T00:54:41Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.237"/>
    <title>v2.1.237</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Fixed prompt caching for sessions using an LLM gateway or custom base URL&lt;/li&gt;
&lt;li&gt;Added a built-in &quot;Concise&quot; output style: Claude leads with results and skips preamble and narration, while doing the work just as thoroughly. Select it under Output style in /config.&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.236</id>
    <updated>2026-08-19T20:02:05Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.236"/>
    <title>v2.1.236</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Added &lt;code&gt;ANTHROPIC_DEFAULT_MODEL&lt;/code&gt; environment variable: sets the model new sessions start on, while a &lt;code&gt;/model&lt;/code&gt; pick still overrides it and persists across restarts (unlike &lt;code&gt;ANTHROPIC_MODEL&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Added &lt;code&gt;notify_when_idle&lt;/code&gt; to cross-session &lt;code&gt;SendMessage&lt;/code&gt;: ask another Claude Code session on this machine to send one notice when it next goes idle — opt-in, one-shot, no polling (macOS and Linux)&lt;/li&gt;
&lt;li&gt;Sandbox: on macOS, wildcard read-deny rules (e.g. &lt;code&gt;**/.env&lt;/code&gt;) now take precedence inside allowed read regions, cover matched directories&#39; contents, and can&#39;t be bypassed by renaming the denied file&lt;/li&gt;
&lt;li&gt;Fixed clipboard copy, background housekeeping, background sessions, and local MCP logs breaking after the directory a session had switched into was removed (since 2.1.229)&lt;/li&gt;
&lt;li&gt;Fixed the fullscreen renderer failing permanently after a single failed start: it now falls back to the classic renderer instead of exiting on every subsequent launch&lt;/li&gt;
&lt;li&gt;Fixed the &lt;code&gt;/model&lt;/code&gt; picker rendering taller than the terminal: it now shows only as many models as fit the window, with the rest reachable by scrolling&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;SendMessage&lt;/code&gt; calls being rejected when a malformed closing tag left the message text inside the summary field&lt;/li&gt;
&lt;li&gt;Fixed unhandled promise rejections when a subprocess fails to start, for example &lt;code&gt;powershell.exe&lt;/code&gt; on WSL with Windows interop disabled (regression in 2.1.234)&lt;/li&gt;
&lt;li&gt;Fixed fullscreen mode sometimes not showing a newly sent message until the next update after the terminal was resized&lt;/li&gt;
&lt;li&gt;Fixed a blank band that could remain above the prompt after clearing a multi-line prompt, and panes not repainting after resizing the terminal away and back, in fullscreen mode&lt;/li&gt;
&lt;li&gt;Fixed the managed-settings approval prompt sometimes not appearing at startup while still capturing the first keypress as approval&lt;/li&gt;
&lt;li&gt;Fixed terminal tab titles jumping in tmux (iTerm tmux integration): the title is now written only when its text changes instead of animating every 960ms&lt;/li&gt;
&lt;li&gt;Fixed an unclear error when the cloud environments list came back empty or malformed&lt;/li&gt;
&lt;li&gt;Fixed the Fable 5 first-time usage-credits prompt auto-selecting the fallback model after 60 seconds with no answer when using Remote Control&lt;/li&gt;
&lt;li&gt;Fixed spinner tips never appearing, with a repeated background error, when the cached guest-pass reward in &lt;code&gt;~/.claude.json&lt;/code&gt; was malformed&lt;/li&gt;
&lt;li&gt;Fixed skills hot-reload in SDK/VS Code sessions raising an error on every skills change after the session&#39;s working directory was deleted (2.1.229+)&lt;/li&gt;
&lt;li&gt;Fixed self-hosted runner sessions released on idle, retire, or startup timeout occasionally resuming on another runner before the post-session hook had finished&lt;/li&gt;
&lt;li&gt;Fixed the Clawd mascot&#39;s eyes and feet rendering unevenly in iTerm2 at some font sizes&lt;/li&gt;
&lt;li&gt;Fixed occasional runaway session recaps: recap text (automatic and &lt;code&gt;/recap&lt;/code&gt;) is now capped at 400 characters, cut at a word boundary&lt;/li&gt;
&lt;li&gt;Improved startup performance: the session counter is now written in the background&lt;/li&gt;
&lt;li&gt;Improved auto mode: &lt;code&gt;Monitor&lt;/code&gt; allow rules are now set aside while auto mode is active, so Monitor commands are reviewed the same way Bash commands are&lt;/li&gt;
&lt;li&gt;Improved auto mode on Bedrock, Vertex AI, and Foundry, and when telemetry is disabled: the classifier now uses the same defaults as on the Claude API, including severity-scored classification&lt;/li&gt;
&lt;li&gt;Improved auto mode: the git status check can no longer be fooled by a repo&#39;s &lt;code&gt;status.showUntrackedFiles=no&lt;/code&gt; setting into reporting a clean tree&lt;/li&gt;
&lt;li&gt;Changed the &lt;code&gt;/model&lt;/code&gt; picker to highlight only the newest model&#39;s name, so the highlight marks the new release rather than an arbitrary subset of the list&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/goal&lt;/code&gt;: an idle session whose goal is parked behind long-running background work now checks in automatically after 30 minutes (then 1h, 2h) instead of waiting for you to return&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/usage&lt;/code&gt; now shows the usage-credits spend row for Team and Enterprise members, and shows a capped row at 0% before anything is spent&lt;/li&gt;
&lt;li&gt;SIGTERM in print/SDK mode no longer records an interrupted turn or synthetic tool denials before exiting; running commands are still terminated and the process still exits with code 143&lt;/li&gt;
&lt;li&gt;Pressing Enter on a slash-command typo or a command unavailable in this session now reports it instead of running the closest fuzzy match; prefixes and aliases still run&lt;/li&gt;
&lt;li&gt;Remote Control now marks a session offline within seconds when the CLI exits or its terminal closes&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SendMessage&lt;/code&gt; now refuses further messages to a session up front once a rapid burst would exceed what that session&#39;s inbox accepts, instead of reporting them sent while they were dropped&lt;/li&gt;
&lt;li&gt;Aligned the session title chip on the prompt border with the footer&#39;s right edge&lt;/li&gt;
&lt;li&gt;Right-aligned footer items (goal indicator, session state, background agent status) and truncated notices now share a consistent right margin with the rest of the prompt area&lt;/li&gt;
&lt;li&gt;[VSCode] Added screen reader support for the transcript: live announcements for replies, permission requests, errors, and status changes, plus per-turn heading navigation&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.235</id>
    <updated>2026-08-18T20:38:54Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.235"/>
    <title>v2.1.235</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Added an optional &lt;code&gt;spellcheck&lt;/code&gt; setting that underlines misspelled words in the prompt input as you type, using your installed &lt;code&gt;aspell&lt;/code&gt;, &lt;code&gt;hunspell&lt;/code&gt;, or &lt;code&gt;ispell&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Fixed whole-prompt-cache invalidation when a language server disconnected or reconnected mid-session&lt;/li&gt;
&lt;li&gt;Fixed nested markdown list items misaligning at depth 3+ and added a hanging indent to wrapped list items in the terminal UI&lt;/li&gt;
&lt;li&gt;Fixed prompt input highlights (slash commands, keywords, mentions) appearing shifted by one or more characters in some multi-line prompts&lt;/li&gt;
&lt;li&gt;Fixed Shift+Tab inside the permission prompt&#39;s comment field approving the edit and granting session-wide edit permission instead of closing the field&lt;/li&gt;
&lt;li&gt;Fixed the Agent tool advertising a general-purpose default in sessions where that agent is unavailable: an omitted &lt;code&gt;subagent_type&lt;/code&gt; there now gets a clear error listing the available agents&lt;/li&gt;
&lt;li&gt;Fixed notebook cell delete/replace approval dialogs silently omitting the existing cell content when the notebook or cell could not be read; the dialog now says why&lt;/li&gt;
&lt;li&gt;Fixed slash commands run while Claude is responding showing HTML entities instead of the actual characters&lt;/li&gt;
&lt;li&gt;Fixed the prompt footer not showing the &quot;Update installed&quot; restart notice after a background auto-update&lt;/li&gt;
&lt;li&gt;Fixed the expanded task list (&lt;code&gt;ctrl+t&lt;/code&gt;) always starting collapsed when resuming or relaunching into a session that still has open tasks&lt;/li&gt;
&lt;li&gt;Improved memory and CPU usage while cloud sessions such as &lt;code&gt;/ultrareview&lt;/code&gt; or &lt;code&gt;/autofix-pr&lt;/code&gt; run in the background — their event streams are no longer re-scanned and re-rendered on every update&lt;/li&gt;
&lt;li&gt;Improved permission dialogs: display text and &quot;don&#39;t ask again&quot; options now always match what a grant would cover, and &quot;don&#39;t ask again&quot; is withheld when contents cannot be fully displayed&lt;/li&gt;
&lt;li&gt;Improved the embedded &lt;code&gt;grep&lt;/code&gt; in native macOS/Linux builds: pathological patterns now fail fast instead of exhausting memory, and &lt;code&gt;-m N&lt;/code&gt; with &lt;code&gt;-A/-C&lt;/code&gt; prints correct context&lt;/li&gt;
&lt;li&gt;Improved the context-limit error to say when auto-compact is off and point to &lt;code&gt;/config&lt;/code&gt; to re-enable it&lt;/li&gt;
&lt;li&gt;Vim mode: NORMAL mode and cursor position are now preserved when toggling the detailed transcript (ctrl+o) or closing a panel&lt;/li&gt;
&lt;li&gt;Dialogs: arrow keys and Enter pressed in quick succession now select the option you navigated to instead of the previously highlighted one&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SendMessage&lt;/code&gt; now refuses messages too large for cross-session delivery up front instead of silently dropping them&lt;/li&gt;
&lt;li&gt;Remote Control: &lt;code&gt;claude rc&lt;/code&gt; now applies the same enterprise-gateway availability check as interactive startup&lt;/li&gt;
&lt;li&gt;[VSCode] Fixed focus jumping between open Claude tabs on its own when a window with several Claude panels is restored or reloaded&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.234</id>
    <updated>2026-08-17T20:20:58Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.234"/>
    <title>v2.1.234</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Added the optional &lt;code&gt;CLAUDE_CODE_PROJECT_DIR_NAME&lt;/code&gt; environment variable: hosts that give each session its own config directory can choose a short name for the per-project transcript directory&lt;/li&gt;
&lt;li&gt;Added the &lt;code&gt;selection:clear&lt;/code&gt; keybinding action, so a key can be bound to clear an in-app text selection; also works in the agents view&lt;/li&gt;
&lt;li&gt;Added a GitLab merge request badge to the footer and statusline: repos with a GitLab remote and an authenticated glab CLI show MR !N with draft/pending/green states&lt;/li&gt;
&lt;li&gt;Claude Code now continues your session automatically when a claude.ai usage limit resets; turn it off in &lt;code&gt;/config&lt;/code&gt; (&quot;Continue automatically at usage limit&quot;)&lt;/li&gt;
&lt;li&gt;Claude is now told to use your account email only to identify you, and not to send it to unrelated services unless you ask&lt;/li&gt;
&lt;li&gt;Security: remote file reads, session restore, CLAUDE.md includes, workflow scripts and file uploads now reject Windows NT-namespace (&lt;code&gt;\??\&lt;/code&gt;) paths, hardening the remaining pre-approval file accesses against the NTLM credential-leak vector&lt;/li&gt;
&lt;li&gt;Fixed auto mode in very long sessions repeatedly re-checking and denying sandboxed commands&#39; network access after the conversation had been compacted&lt;/li&gt;
&lt;li&gt;Fixed session-scoped permission answers (including denies) being dropped when answering background subagent tool permission prompts&lt;/li&gt;
&lt;li&gt;Fixed a crash when an API response on the non-streaming fallback path (typically via third-party gateways) contained a thinking block missing its thinking field or a text block missing its text field&lt;/li&gt;
&lt;li&gt;Fixed markdown rendering becoming extremely slow for some messages containing unusual Unicode sequences&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;SendMessage&lt;/code&gt; rejecting a recipient copied from &lt;code&gt;ListAgents&lt;/code&gt; when the session name is at the 200-character cap or emoji-heavy&lt;/li&gt;
&lt;li&gt;Fixed repository detection mis-reading the host of git remotes with unusual userinfo, producing links and repo-specific behavior for the wrong host&lt;/li&gt;
&lt;li&gt;Fixed MCP diagnostics printing resolved secrets: scope-conflict warnings now show the configured &lt;code&gt;${VAR}&lt;/code&gt; form, and connection-failure details show only the server origin&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;strictKnownMarketplaces&lt;/code&gt; allowlists accepting SCP-style git marketplace sources whose host differs from the one git would actually connect to&lt;/li&gt;
&lt;li&gt;Fixed modal text such as the &lt;code&gt;/login&lt;/code&gt; OAuth URL losing characters when copied in fullscreen&lt;/li&gt;
&lt;li&gt;Fixed a &lt;code&gt;---&lt;/code&gt; horizontal rule in rendered markdown running into the line after it&lt;/li&gt;
&lt;li&gt;Fixed consecutive shell commands splitting into multiple &quot;Ran 1 shell command&quot; rows when todo/task updates were interleaved between them&lt;/li&gt;
&lt;li&gt;Fixed dialogs like &lt;code&gt;/permissions&lt;/code&gt; opened while a &lt;code&gt;!&lt;/code&gt; shell command was running being dismissed when the command finished&lt;/li&gt;
&lt;li&gt;Fixed a queued &lt;code&gt;!&lt;/code&gt; shell command being sent to the model as plain text after pressing up-arrow to edit the queued input&lt;/li&gt;
&lt;li&gt;Fixed queued messages reappearing in the prompt history while still queued, Esc while selecting a queued message no longer interrupts the turn, and &lt;code&gt;!&lt;/code&gt; mode no longer sticks after a mid-turn submit&lt;/li&gt;
&lt;li&gt;Fixed accepting the &quot;Try the new fullscreen renderer?&quot; prompt restarting the session without its permission mode (e.g. &lt;code&gt;--dangerously-skip-permissions&lt;/code&gt;), tool allow/deny rules, model or effort flags&lt;/li&gt;
&lt;li&gt;Fixed &lt;code&gt;/tui&lt;/code&gt; dropping launch &lt;code&gt;--allowed-tools&lt;/code&gt;/&lt;code&gt;--disallowed-tools&lt;/code&gt; rules when it restarts; it now declines to switch, with the reason, when the session has restrictions a restart can&#39;t carry over&lt;/li&gt;
&lt;li&gt;Fixed trust prompts omitting the repository-wide scope warning when the directory was first seen before the repository existed there&lt;/li&gt;
&lt;li&gt;Fixed a case where an IDE diff tab closing during a permission re-prompt could answer the new prompt with the previous input&lt;/li&gt;
&lt;li&gt;Fixed: files sent to the user during Remote Control sessions hosted by Claude Code Desktop or VS Code now upload, so they open on phone and web instead of showing an empty card&lt;/li&gt;
&lt;li&gt;Fixed: after &lt;code&gt;/login&lt;/code&gt; while &lt;code&gt;CLAUDE_CODE_OAUTH_TOKEN&lt;/code&gt; is set, the stale-token reminder no longer leaks into Claude&#39;s automatically resumed turn — it now appears only to you&lt;/li&gt;
&lt;li&gt;Fixed: permission previews now relay only to channel servers admitted by the inbound trust gate, and a server&#39;s explicit permission-capability opt-out is honored&lt;/li&gt;
&lt;li&gt;Fixed: credential masking on relayed permission previews can no longer hide commands, paths, or destinations from the approver; oversized private-key blocks now redact under full-strength redaction&lt;/li&gt;
&lt;li&gt;Fixed: provider API tokens that mask on permission previews now mask even when directly followed by shell delimiters&lt;/li&gt;
&lt;li&gt;Fixed Claude Desktop inter-session messages being silently dropped by the recipient session when cross-session messaging read as disabled, which left the sender&#39;s query &quot;thinking&quot; for many minutes&lt;/li&gt;
&lt;li&gt;Remote Control: signing this computer in to a different claude.ai account or organization now stops the running session within seconds and says why, instead of a misleading HTTP 404 hours later&lt;/li&gt;
&lt;li&gt;Remote Control sessions started from Claude Code Desktop or VS Code now keep phones and claude.ai/code updated on the session&#39;s permission mode (and claude.ai/code on the model) as they change&lt;/li&gt;
&lt;li&gt;Remote Control: effort picks made on a phone or on claude.ai/code now apply to terminal- and Desktop/VS Code-hosted sessions, and the session publishes its effort level to connected clients&lt;/li&gt;
&lt;li&gt;&lt;code&gt;SendMessage&lt;/code&gt; and &lt;code&gt;ListAgents&lt;/code&gt; now say when your account&#39;s session list was too long to check completely, instead of treating unseen sessions as absent&lt;/li&gt;
&lt;li&gt;Expired Anthropic profile credential now points you at &lt;code&gt;/login&lt;/code&gt; when a claude.ai login would take precedence&lt;/li&gt;
&lt;li&gt;Improved the transcript: your own prompts now render markdown (highlighted code blocks, inline code, lists) the same way replies do&lt;/li&gt;
&lt;li&gt;Improved the &quot;API returned an empty or malformed response&quot; error to say what came back (content type, body kind, size, request ID) and why the original streaming request failed&lt;/li&gt;
&lt;li&gt;Improved auto-generated session titles to read as short, specific names (e.g. &quot;Login button bug&quot;) rather than sentences restating your request (e.g. &quot;Fix the login button on mobile&quot;)&lt;/li&gt;
&lt;li&gt;Reduced the context cost of loading the built-in &lt;code&gt;claude-api&lt;/code&gt; skill from ~200k+ tokens to ~25k by loading reference docs on demand&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/permissions&lt;/code&gt; can now be opened while Claude is working — rule changes apply to the rest of the current turn&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/add-dir &amp;lt;path&amp;gt;&lt;/code&gt; can now be used while Claude is working; &lt;code&gt;/add-dir&lt;/code&gt;, &lt;code&gt;/autocompact&lt;/code&gt;, &lt;code&gt;/theme&lt;/code&gt;, &lt;code&gt;/help&lt;/code&gt;, &lt;code&gt;/config&lt;/code&gt; and &lt;code&gt;/advisor&lt;/code&gt; dialogs open mid-turn in the fullscreen TUI&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/goal&lt;/code&gt; now clears itself with a notice when a turn dies on an unrecoverable error (e.g. revoked auth, an exhausted credit balance, or a context overflow) instead of staying armed&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/goal&lt;/code&gt;: when background tasks keep a goal waiting for 30+ minutes, Claude now checks in on them instead of waiting indefinitely (set &lt;code&gt;CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0&lt;/code&gt; to opt out)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;claude setup-token&lt;/code&gt; now rejects unexpected extra arguments instead of silently ignoring them&lt;/li&gt;
&lt;li&gt;Changed Esc in fullscreen mode to no longer clear a mouse text selection: it interrupts or dismisses as usual and the selection stays highlighted&lt;/li&gt;
&lt;li&gt;Removed the redundant &quot;Allowed by auto mode classifier&quot; line that auto mode showed under every Agent tool call&lt;/li&gt;
&lt;li&gt;Removed the &quot;Default teammate model&quot; setting from &lt;code&gt;/config&lt;/code&gt;; agent-team teammates now use the leader&#39;s model unless the spawn names one&lt;/li&gt;
&lt;li&gt;Dimmed the elapsed-time counter on the running tool header so it no longer competes with the bold counts&lt;/li&gt;
&lt;li&gt;Background task notifications delivered between turns are now sent to the model inside &lt;code&gt;&amp;lt;system-reminder&amp;gt;&lt;/code&gt; tags, matching mid-turn delivery&lt;/li&gt;
&lt;li&gt;Mantle: skip the admin-pin availability probe at startup when a main-loop model is already picked&lt;/li&gt;
&lt;li&gt;Windows: startup no longer stalls on repeated rename retries when &lt;code&gt;~/.claude.json&lt;/code&gt; is read-only&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
  <entry>
    <id>tag:github.com,2008:Repository/937253475/v2.1.233</id>
    <updated>2026-08-14T22:20:57Z</updated>
    <link rel="alternate" type="text/html" href="https://github.com/anthropics/claude-code/releases/tag/v2.1.233"/>
    <title>v2.1.233</title>
    <content type="html">&lt;h2&gt;What&#39;s changed&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Added GitLab merge request URL support to the &lt;code&gt;--worktree&lt;/code&gt; flag and the &lt;code&gt;claude agents&lt;/code&gt; view (where MRs display as &lt;code&gt;!N&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Added an opt-in &lt;code&gt;forward_user_identity&lt;/code&gt; apps gateway setting on Anthropic upstreams that sends the signed-in user&#39;s identity as headers, so a proxy behind the gateway can attribute spend per user&lt;/li&gt;
&lt;li&gt;Added opt-in memory cgroup support for Bash tool commands on Linux (&lt;code&gt;CLAUDE_CODE_TOOL_MEMORY_LIMIT&lt;/code&gt;) so a runaway build can&#39;t stall the session&lt;/li&gt;
&lt;li&gt;Added &lt;code&gt;CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS&lt;/code&gt; environment variable to configure the WebFetch session URL cache TTL (default unchanged: 15 minutes)&lt;/li&gt;
&lt;li&gt;Fixed cloud sessions occasionally being marked as lost when the environment shut down while Claude was waiting on a permission prompt&lt;/li&gt;
&lt;li&gt;Fixed MCP v2 connections endlessly reopening the subscriptions/listen stream against servers that terminate long-held streams on a fixed timeout (e.g. serverless hosts)&lt;/li&gt;
&lt;li&gt;Fixed Notification hooks not firing for permission prompts when running under Claude Desktop or VS Code&lt;/li&gt;
&lt;li&gt;Fixed idle sessions on Linux sometimes keeping one CPU core at 100% when sandboxing is enabled&lt;/li&gt;
&lt;li&gt;Fixed bundled skill aliases like &lt;code&gt;/checkup&lt;/code&gt; and &lt;code&gt;/review&lt;/code&gt; reporting &quot;Unknown command&quot; in &lt;code&gt;-p&lt;/code&gt; mode or with plugins/MCP loaded when a user or project skill shadows the bundled skill&lt;/li&gt;
&lt;li&gt;Fixed skill/command argument substitution to prevent argument values from being re-expanded as template markers&lt;/li&gt;
&lt;li&gt;Fixed Windows paths spelled with the NT &lt;code&gt;\??\&lt;/code&gt; device prefix bypassing UNC path validation, closing an NTLM credential-leak vector&lt;/li&gt;
&lt;li&gt;Improved &lt;code&gt;claude self-hosted-runner&lt;/code&gt; session start time: the session branch is now created without rewriting the working tree, and two server round trips no longer block the agent&#39;s launch&lt;/li&gt;
&lt;li&gt;Improved apps gateway error forwarding: 400/413 errors from Vertex, Foundry, and Claude Platform on AWS upstreams now carry the upstream&#39;s own message; fixes a bug with auto-compact on apps gateway&lt;/li&gt;
&lt;li&gt;Improved &lt;code&gt;claude plugin validate&lt;/code&gt; to check a bare &lt;code&gt;.claude/skills&lt;/code&gt; directory, reporting SKILL.md files whose frontmatter fails to parse&lt;/li&gt;
&lt;li&gt;Improved screen reader mode: the &lt;code&gt;/effort&lt;/code&gt; selector renders as a numbered list with a typed-number prompt, and hint and dialog text is no longer clipped&lt;/li&gt;
&lt;li&gt;Improved print mode diagnostics: a &lt;code&gt;[claude-code:unrecognized_model]&lt;/code&gt; line is written to stderr when a request goes out for a model ID Claude Code doesn&#39;t recognize; map it with &lt;code&gt;modelOverrides&lt;/code&gt; to silence&lt;/li&gt;
&lt;li&gt;Changed the GitHub app setup tip to no longer appear in repositories whose origin remote is on gitlab.com or bitbucket.org; the enterprise marketplace tip now covers non-GitHub internal git hosts&lt;/li&gt;
&lt;li&gt;Todo/task-tracking tools (TaskCreate/Get/Update/List, TodoWrite) are no longer available on Opus 4.8, Sonnet 5, Fable 5, Mythos 5, and newer models; set &lt;code&gt;CLAUDE_CODE_ENABLE_TODO_TOOLS=1&lt;/code&gt; to bring them back&lt;/li&gt;
&lt;li&gt;Windows: fixed auto mode repeatedly stopping for manual approval on ordinary &lt;code&gt;cd &amp;lt;dir&amp;gt; &amp;amp;&amp;amp; &amp;lt;command&amp;gt; &amp;gt; file&lt;/code&gt; Bash commands (a 2.1.232 regression)&lt;/li&gt;
&lt;li&gt;Reverted the 2.1.232 Bash permission changes for Cygwin-style symlinks on Windows and for input redirections (&lt;code&gt;&amp;lt; file&lt;/code&gt;); a narrower version will return in a later release&lt;/li&gt;
&lt;/ul&gt;</content>
    <author>
      <name>ashwin-ant</name>
    </author>
    <media:thumbnail height="30" width="30" url="https://avatars.githubusercontent.com/u/178951676?s=60&amp;v=4"/>
  </entry>
</feed>
